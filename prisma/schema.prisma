// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://scouts_aanwezigheden_db_71lq_user:9oWfT0rD17dG5E6zqsSHAYlu0QaJ29N8@dpg-cr3n530gph6c73ffdu20-a.frankfurt-postgres.render.com/scouts_aanwezigheden_db_71lq"
  //url      = env("DATABASE_URL")
}


model Member {
  id        String   @id @default(uuid())
  firstName String
  lastName  String
  group     Group
  activities Activity[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("members")
}

model Activity {
  id        String   @id @default(uuid())
  name      String
  date      DateTime @default(now())
  members   Member[]
  group     Group
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("activities")
}

enum Group {
  KAPOENEN
  WOUTERS
  JONGGIVERS
  GIVERS
  JINS
}