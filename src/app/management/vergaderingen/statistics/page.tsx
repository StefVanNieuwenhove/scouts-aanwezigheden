import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';
import React from 'react';

const VergaderingenStatPage = () => {
  return (
    <Alert variant='warning' className='mt-4'>
      <AlertTitle>WIP</AlertTitle>
      <AlertDescription>
        Deze functionaliteit is nog niet geimplementeerd
      </AlertDescription>
    </Alert>
  );
};

export default VergaderingenStatPage;
